# Core dependencies
pandas>=1.5.0
numpy>=1.24.0

# Crypto market data
ccxt>=4.4.0
fredapi>=0.5.2
# Optional TradingView macro feed. Install from its upstream source when needed;
# the runtime treats it as optional and falls back gracefully if unavailable.
# tvdatafeed

# Configuration
pyyaml>=6.0

# Scheduling
schedule>=1.2.0

# Logging
loguru>=0.7.0

# HTTP clients
requests>=2.28.0
aiohttp>=3.8.0
openai>=1.0.0

# Database
# Runtime storage is SQLite through Python's stdlib sqlite3.
# PostgreSQL migration is planned but not active in this build.

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
websockets>=12.0
python-multipart>=0.0.6
httpx>=0.27.0

# Desktop packaging
pyinstaller>=6.0.0

# Local encrypted credential storage.
cryptography>=42.0.0
